server {
  listen 80;
  location / {
    root /var/www/html;
    index  index.html index.htm;
    try_files $uri $uri/ /index.html;
  }

  location ~ ^/dist/(.+)/$ {
    try_files $uri $uri/ /dist/$1;
  }

  location /api {
    proxy_pass http://13.209.169.93:8081;
  }
}
